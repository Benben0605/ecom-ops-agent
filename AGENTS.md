# AGENTS.md

## 项目使命

ecom-ops-agent 是面向私域电商的**评估驱动 Agent 工程项目**，当前不是生产系统。项目要在可控、版本化的
业务数据上打通“自然语言请求 → 工具编排 → 基于证据的回复 → 调用审计 → L1/L2 评估 → 实验迭代”，并用
同一评估基线验证架构演进。

顾客侧聚焦订单、售后政策、商品推荐和必要的人工转接；商家侧聚焦基于订单数据的运营分析。合成数据与评估
分数只证明当前基线上的行为，不代表真实用户效果、生产稳定性或业务收益。扩展必须来自明确业务问题和可证伪假设，不以增加框架、工具或 Agent 数量本身为目标。

## 协作约定

- 开始实现、修改行为或调整评估口径前，先读 [`docs/specs/`](./docs/specs/) 中的相关模块规则与设计规格，
  再用 `rg` 查实现、调用方、数据契约和测试。
- 规格描述意图，代码描述现状，测试与实验提供证据。三者冲突时先定位差异，不静默选择一边；没有实现与验证
  证据的能力不得描述为可用。
- 从失败 case、用户可见行为或契约缺口提出可证伪假设，做最小变更，并补最接近边界的验证；不要顺手重写无关模块。
- 系统边界、工具契约、权限、安全、评估口径或跨模块产物发生变化时，同步相关规格和
  索引。模块级规则、设计细节和状态说明放在 `docs/specs/`，不堆回本文件。
- 保留用户已有改动；不得提交或记录真实密钥、完整授权头、敏感用户数据及不可公开的内部信息。

## 模型与主线

- 默认主线是 `ChatSession` 的单 Agent ReAct/tool-calling loop；`SupervisorAgent` 仅用于对照实验，未经新评估证据不得切为默认路径。
- 默认架构不得在没有等价性基线、迁移 Spec 和评估证据时切换。
- 可信身份与权限由执行层注入和校验，prompt 不是安全边界；最终回复只能使用工具、检索或版本化业务数据明确提供的事实。无法可靠完成时应澄清、拒答或转人工，不得猜测。
- L1 与 L2 保持正交：L1 判断业务工具调用 multiset，L2 判断要点命中与相对工具输出的忠实度。模型或 judge的非确定性结论不得用单次运行裁决。

## 管理与发布

- 分支、Issue、PR 和实验生命周期以 [`CONTRIBUTING.md`](./CONTRIBUTING.md) 为准；`main` 保存已收口里程碑，
  开发分支的变更通过可追溯实验和评审进入主线。
- 发布或合并前先跑受影响的确定性测试；涉及模型、prompt、工具描述或 judge 时，再运行最小相关评估并记录假设、数据集版本、参数、结果和裁决。非确定性实验默认至少 `N≥4`。
- 不兼容契约变更必须提升 schema 版本，重新生成并审查快照，同时明确旧产物的迁移、重跑或删除策略。
- 用户可见变化写入 `CHANGELOG.md`，长期架构取舍写入 `docs/decisions/` 下的 ADR，模块状态写入规格；
  README 中的 case 数、指标和演示结果不是永久承诺。
- 依赖、容器与部署配置必须保持一致；可再生产物不得入库。具体安装、测试、评估和发布命令以
  [`README.md`](./README.md)、[`CONTRIBUTING.md`](./CONTRIBUTING.md) 及对应规格为准。
