def run(query: str) -> str:
    # TODO(你来写 · Week1-Step4)：基础 RAG 检索
    #   1. 加载 data/faq/ 下的文档并切分成块
    #   2. embedding + 存向量库（如 chroma），首次构建后可缓存
    #   3. 用 query 检索 top-k，拼成上下文字符串返回
    raise NotImplementedError("Week1-Step4：在这里实现 kb_search 的 RAG 检索")
